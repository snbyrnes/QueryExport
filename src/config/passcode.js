/**
 * Passcode configuration.
 * In production, you'd validate against a server-side hash.
 * This is a simple client-side gate for public access control.
 *
 * Change this value to set your access passcode.
 */
export const PASSCODE = 'uptipp'
